import React from 'react'
import Helmet from 'react-helmet'

const Layout = ({children}) => {
  return (
    <>
      <Helmet>

      </Helmet>
        {children}
    
    
    </>
  )
}

export default Layout
